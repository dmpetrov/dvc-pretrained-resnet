objects:
    resnet50:
        summon:
            type: python
            call: resnet.resnet50
            deps: ['resnet50.pth']

    resnet18:
        summon:
            type: python
            call: resnet.resnet18
            deps: ['resnet18.pth']

