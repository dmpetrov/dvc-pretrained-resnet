objects:
    resnet50:
        summon:
            type: python
            call: resnet.resnet50
            args:
                pretrained: True
                weights_file: resnet50.pth

    resnet18:
        summon:
            type: python
            call: resnet.resnet18
            args:
                pretrained: True
                weights_file: resnet18.pth

